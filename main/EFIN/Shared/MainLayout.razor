@inherits LayoutComponentBase

<RadzenDialog />
<RadzenNotification />
<Toast />


<MatDrawerContainer  >
    <MatDrawer @bind-Opened="@Opened" Mode="@MatDrawerMode.Modal" @ref="MenuLateral" Style="width:30%;">
        <NavMenu />
    </MatDrawer>
    <MatDrawerContent>
        <MenuBar MatDrawer="MenuLateral" />
        @Body
    </MatDrawerContent>
</MatDrawerContainer>


@code{

    private MatDrawer MenuLateral { get; set; }

    bool Opened = false;

}
